# algorithm

- Search(탐색)
  - Linear search: O(n), JS의 indexOf, includes, findIndex, find
  - Binary search: O(log n)
  - Substring Counting: O(n \* m)
- Comparison Sort(비교 정렬)
  - Bubble sort: O(n^2)

# data structure
- Single Linked List(단일 연결 리스트)


## coding test
